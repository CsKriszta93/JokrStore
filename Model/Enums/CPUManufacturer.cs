using System.ComponentModel.DataAnnotations;

namespace Model.Enums
{
    public enum CPUManufacturer
    {
        [Display(Name = "AMD")]
        AMD,

        [Display(Name = "Intel")]
        Intel
    }
}
